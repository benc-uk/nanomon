apiVersion: v1
kind: Service
metadata:
  name: { { include "nanomon.fullname" . } }
  labels: { { - include "nanomon.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "nanomon.selectorLabels" . | nindent 4 } }
