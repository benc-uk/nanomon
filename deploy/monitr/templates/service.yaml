apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitr.fullname" . }}
  labels:
    {{- include "monitr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monitr.selectorLabels" . | nindent 4 }}
