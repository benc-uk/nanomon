apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nanomon.fullname" . }}-test-connection"
  labels:
    {{- include "nanomon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nanomon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
