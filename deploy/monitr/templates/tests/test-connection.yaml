apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monitr.fullname" . }}-test-connection"
  labels:
    {{- include "monitr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monitr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
